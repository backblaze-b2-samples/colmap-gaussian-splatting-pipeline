# Tech debt

## 2026-08-06 — verify

- Artifacts on B2 (capture detail) — every artifact shows the identical version label `v4_z1e2d2` → the UI truncates the B2 versionId to its first 8 chars, which is a shared bucket-scoped prefix, so distinct object versions render indistinguishably (the value is real, just non-informative as shown) (.local/verify/C/R1-05-detail-done.png)
- Sparse model bundle — `points3D.txt` is read in `services/api/app/repo/sfm.py` but not uploaded; the sparse model is staged as `cameras.txt` + `images.txt` only (the 3D points ship via `points.ply`, so the goal's "sparse model + points.ply" is still satisfied) → a strict COLMAP text model is three files (add `points3D.txt` for round-trip fidelity) (.local/verify/C2/defaults-04-detail-terminal.png)
- Capture detail during Run (first ~7–10s) — the stage timeline is gated on `stages.length > 0`, which is empty until the first streamed snapshot arrives, so immediately after pressing Run the user briefly sees only the "Reconstructing…" spinner + toast with no stage breakdown or progress bar → render the initial pending-stage timeline immediately on `running` to close the gap (self-resolves within ~2s) (.local/verify/A3/06-running.png)
- Capture detail during Run (finalize tail) — the determinate progress bar can reach "7 of 7 stages / 100%" while a ~30–90s B2 artifact-upload tail is still running under status `running` (no stage represents the upload) → the bar overstates completion for the tail; the live elapsed timer keeps advancing so it does not look frozen, but consider adding an explicit "Stage artifacts to B2 / finalize" stage so the bar reflects the remaining upload work (gate verdict REFUTED as a friction — the cited frame showed the Done state — logged here as a refinement) (.local/verify/B2/12-done-detail.png)
